# ============================================
# ZaloAssistant Backend - Requirements
# Cleaned and optimized for Docker deployment
# ============================================

# --- Core Web Framework ---
quart>=0.20.0

# --- HTTP Clients ---
aiohttp>=3.9.0
httpx>=0.27.0
aiofiles>=24.1.0

# --- Scheduling ---
apscheduler>=3.10.0

# --- Database ---
tinydb>=4.8.0

# --- Configuration Management ---
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# --- Google AI (Gemini) ---
google-generativeai>=0.8.0

# --- Logging & Utilities ---
colorama>=0.4.6

# --- Knowledge Pipeline (Optional - for offline rebuild) ---
# langextract: structured entity extraction from documents
# Install for enhanced knowledge extraction: pip install langextract
# PageIndex: tree-based document indexing (optional, has built-in fallback)
# Install for native PageIndex: pip install PyPDF2 pymupdf tiktoken pyyaml